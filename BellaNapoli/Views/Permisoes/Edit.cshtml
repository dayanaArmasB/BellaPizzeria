@model BellaNapoli.Models.Permiso

@{
    ViewData["Title"] = "Editar Permiso";
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <title>@ViewData["Title"]</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Segoe+UI&display=swap" rel="stylesheet">
    <style>
        html, body {
            height: 100%;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #fffafa;
            overflow-x: hidden; /* Previene scroll horizontal */
        }

        .container-editar {
            flex: 1;
            padding: 40px 20px;
            max-width: auto;
            margin: 40px auto;
            background-color: #fff;
            border-radius: 10px;
            margin-bottom: 5rem;
            box-shadow: 0 0 15px rgba(0, 123, 255, 0.1);
            border: 2px solid #007bff;
        }

        h1 {
            text-align: center;
            font-size: 2.2rem;
            color: #007bff;
            margin-bottom: 10px;
        }

        h4 {
            text-align: center;
            font-size: 1.3rem;
            margin-bottom: 20px;
            color: #555;
        }

        hr {
            margin: 20px 0;
            border-top: 2px solid #007bff;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            font-weight: 600;
        }

        .form-buttons {
            margin-top: 20px;
            text-align: center;
        }

        .form-control {
            border-radius: 5px;
            padding: 10px;
        }
    </style>
</head>
<body>
    <div class="container-editar">
        <h1>Editar Permiso</h1>

        <h4>Modificar información del Permiso</h4>
        <hr />
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdPermiso" />

            <div class="form-group">
                <label asp-for="IdRol" class="control-label"></label>
                <select asp-for="IdRol" class="form-control" asp-items="ViewBag.IdRol"></select>
            </div>

            <div class="form-group">
                <label asp-for="NombreMenu" class="control-label"></label>
                <input asp-for="NombreMenu" class="form-control" />
                <span asp-validation-for="NombreMenu" class="text-danger"></span>
            </div>

            <div class="form-control">
                <input type="submit" value="Guardar cambios" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">← Volver a la lista</a>
            </div>
        </form>
    </div>

    @section Scripts {
        @{
            await Html.RenderPartialAsync("_ValidationScriptsPartial");
        }
    }
</body>
</html>

